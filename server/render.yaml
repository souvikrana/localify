services:
  - type: web
    name: localify-extractor
    runtime: image
    image:
      url: https://github.com/souvikrana/localify.git
      dockerfilePath: server/Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "8000"
